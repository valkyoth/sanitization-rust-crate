[package]
name = "core-dump-probe"
version = "0.0.0"
edition = "2021"
publish = false

[workspace]

[dependencies]
sanitization = { version = "2.1.0", path = "../../crates/sanitization", features = ["memory-lock"] }
