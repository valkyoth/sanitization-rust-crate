[package]
name = "lifecycle-probes"
version = "0.0.0"
edition = "2021"
publish = false

[workspace]

[dependencies]
sanitization = { version = "2.1.0", path = "../../crates/sanitization", features = ["alloc"] }
